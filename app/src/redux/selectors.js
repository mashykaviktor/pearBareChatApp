export const getMessages = (state) => state.messages || [];

export const getInputText = (state) => state.inputText;

export const getRoomTopic = (state) => state.roomTopic;

export const getRoomTopicIn = (state) => state.roomTopicIn;

export const getPeersCount = (state) => state.peersCount;
